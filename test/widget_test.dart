import 'package:flutter_test/flutter_test.dart';

import 'package:quickclix/main.dart';

void main() {
  test('QuickclixApp can be created', () {
    const app = QuickclixApp();
    expect(app, isNotNull);
  });
}
